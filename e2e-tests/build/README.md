# Build Certificates

Place HSBC certificates here before building Docker images:

- `hsbc_root.cer` — HSBC root CA certificate
- `truststore.pem` — Combined trust store

These files are `.gitignored` and must be obtained from the HSBC PKI team.
